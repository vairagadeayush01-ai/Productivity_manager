diff --git a/practice_programs/backend/routes/search.py b/practice_programs/backend/routes/search.py
index 1234567..abcdefg 100644
--- a/practice_programs/backend/routes/search.py
+++ b/practice_programs/backend/routes/search.py
@@ -1,6 +1,6 @@
 from fastapi import APIRouter, Query, Depends
 from sqlalchemy.orm import Session
-from datetime import date as date_type
+from datetime import date as date_type, datetime, time
 from database import get_db, LearningEntry
 from services import vector_store
 
@@ -25,13 +25,14 @@ async def search_entries(q: str = Query(...), n: int = Query(5, ge=1, le=20))
 
 @router.get("/today")
 async def get_today(db: Session = Depends(get_db)):
     today   = date_type.today()
+    today_start = datetime.combine(today, time.min)
     entries = db.query(LearningEntry).filter(
-        LearningEntry.created_at >= today.isoformat()
+        LearningEntry.created_at >= today_start
     ).order_by(LearningEntry.created_at.desc()).all()
     return {"date": today.isoformat(), "count": len(entries), "entries": [
         {"id": e.id, "source_type": e.source_type, "title": e.title,
-         "summary": e.summary, "topics": e.topics.split(", ") if e.topics else [],
+         "summary": e.summary, "topics": (e.topics.split(", ") if e.topics else []),
          "created_at": e.created_at.isoformat()}
         for e in entries
     ]}
@@ -42,10 +43,10 @@ async def get_history(skip: int = 0, limit: int = 50, db: Session = Depends(g
     entries = db.query(LearningEntry).order_by(LearningEntry.created_at.desc()).offset(skip).limit(limit).all()
     total = db.query(LearningEntry).count()
     return {"total": total, "entries": [
         {"id": e.id, "source_type": e.source_type, "title": e.title,
-         "summary": e.summary, "topics": e.topics.split(", ") if e.topics else [],
+         "summary": e.summary, "topics": (e.topics.split(", ") if e.topics else []),
          "created_at": e.created_at.isoformat()}
         for e in entries
     ]}


SUMMARY OF CHANGES:
===================

1. Line 3: Added imports 'datetime' and 'time'
   - from datetime import date as date_type, datetime, time
   - Needed for proper datetime comparison in /today endpoint

2. Line 29-31: Fixed /today endpoint date filtering
   - Added: today_start = datetime.combine(today, time.min)
   - Changed: LearningEntry.created_at >= today.isoformat()
             to: LearningEntry.created_at >= today_start
   - Reason: Proper DateTime to DateTime comparison (was DateTime to String)

3. Line 35: Fixed /today endpoint topics handling
   - Changed: "topics": e.topics.split(", ") if e.topics else []
             to: "topics": (e.topics.split(", ") if e.topics else [])
   - Reason: Operator precedence - added parentheses for correct order

4. Line 47: Fixed /history endpoint topics handling
   - Changed: "topics": e.topics.split(", ") if e.topics else []
             to: "topics": (e.topics.split(", ") if e.topics else [])
   - Reason: Safe null handling - prevents AttributeError on None


AFFECTED ENDPOINTS:
===================
- GET /search/today - Now filters correctly from midnight
- GET /search/history - Now handles NULL topics without crashing


BACKWARD COMPATIBILITY:
======================
✓ No changes to API contract
✓ No changes to request parameters
✓ No changes to response structure
✓ Same behavior for existing valid data
✓ Improved behavior for NULL/empty data


TESTING RECOMMENDATIONS:
=======================
1. Test with entries that have NULL topics
2. Test with entries that have empty string topics
3. Test pagination with /history endpoint
4. Test /today endpoint with entries from today
5. Verify no exceptions in server logs


DEPLOYMENT NOTES:
================
- No database migrations needed
- No configuration changes needed
- Safe to deploy immediately
- Restart backend service after deployment
- Monitor server logs for any issues
